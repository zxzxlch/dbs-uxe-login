import { ButtonContentDirective } from './button-content.directive';

describe('ButtonContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonContentDirective();
    expect(directive).toBeTruthy();
  });
});
